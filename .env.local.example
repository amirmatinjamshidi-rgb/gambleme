# Copy this file to .env.local and set your own values.
# Never commit .env.local — it is gitignored.

# Used to sign anonymous session cookies (HMAC). Use a long random string in production.
SESSION_SECRET=replace-with-a-long-random-string-at-least-32-characters
